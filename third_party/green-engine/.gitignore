# Rust
/target
**/target/
**/*.rs.bk
# Built native kernel library (produced by `make -C crates/kernels cuda`)
**/libgreen_engine_kernels.so

# Python
__pycache__/
*.pyc
.venv/
venv/
.venv-embed/
.venv-*/

# Local agent / editor state (generated by codehelper init, Cursor, Claude Code — do not commit)
.claude/
.cursor/
.vscode/
.codehelper/
.mcp.json
AGENTS.md
CLAUDE.md

# Local-only research / trace capture (not part of the published repo)
experiments/
idea.md
results/
patch_all.py
scripts/_apply_*.py
scripts/apply_*.py
scripts/patch_*.py

# Local model weights and GE_HOME artifacts (never commit)
*.gguf
.green/
**/embed-onnx/

# Local tool caches / shallow clones (llama.cpp src, CUDA bins, venvs)
.tools/
