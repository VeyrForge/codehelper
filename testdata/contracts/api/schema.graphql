# stub GraphQL schema for agent locate / projects contracts
type Order {
  id: ID!
  total: Float!
}

type Query {
  order(id: ID!): Order
  orders: [Order!]!
}

type Mutation {
  createOrder(total: Float!): Order
}