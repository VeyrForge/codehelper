services:
  db:
    image: postgres:16-alpine
    volumes:
      - pgdata:/var/lib/postgresql/data
  api:
    build: .
    depends_on:
      - db
  web:
    image: nginx:alpine
    depends_on: [api]

networks:
  front:

volumes:
  pgdata:
