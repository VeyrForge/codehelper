syntax = "proto3";

package demo.common;

// Shared types imported by user.proto (import densify probe).
message Timestamp {
  int64 seconds = 1;
  int32 nanos = 2;
}

enum Status {
  STATUS_UNSPECIFIED = 0;
  STATUS_ACTIVE = 1;
  STATUS_INACTIVE = 2;
}
