import Callout from './Callout.mdx'
import { Note as Hint } from './Note.mdx'

export function highlight(text) {
  return text.toUpperCase()
}

# Intro

Welcome — render <Callout /> and <Hint /> plus {Badge}.

```ts
export function fence() { return 1 }
```
