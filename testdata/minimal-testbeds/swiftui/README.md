# SwiftUI stub bed

Paired probe: `HomeView` → `DetailView` / `GreetingView`; `DetailScreen` role=screen.

- `Views/HomeView.swift` — NavigationStack + NavigationLink → DetailView + GreetingView
- `Views/DetailView.swift` — destination view
- `Views/GreetingView.swift` — child view
