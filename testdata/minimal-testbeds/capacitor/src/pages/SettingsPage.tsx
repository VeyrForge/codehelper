import { IonPage, IonContent } from "@ionic/react";

/** SettingsPage — Ionic leaf page. */
export function SettingsPage() {
  return (
    <IonPage>
      <IonContent>settings</IonContent>
    </IonPage>
  );
}
